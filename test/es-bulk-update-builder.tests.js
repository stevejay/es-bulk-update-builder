'use strict';

// const should = require('should');
const BulkUpdateBuilder = require('../index');

describe('es-bulk-update-builder', function() {
    it('should construct', function() {
        new BulkUpdateBuilder();
    });
});
